# Supabase 接続設定（コピーして .env を作る）。
# EXPO_PUBLIC_ 接頭辞の変数は Expo がビルド時にクライアントへ埋め込む「公開変数」。
# publishable key は公開前提（データ保護は DB 側の RLS が担う）。secret key はここに置かない。

EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
